-- Ejecutar estos comandos uno por uno en MySQL Workbench o línea de comandos

USE cargas_trabajo;

-- 1. Verificar estructura actual
DESCRIBE tiempos_procedimientos;

-- 2. Verificar si existen las columnas de tiempo
SELECT 
    COLUMN_NAME 
FROM INFORMATION_SCHEMA.COLUMNS 
WHERE TABLE_SCHEMA = 'cargas_trabajo' 
  AND TABLE_NAME = 'tiempos_procedimientos'
  AND COLUMN_NAME IN ('tiempo_minimo', 'tiempo_promedio', 'tiempo_maximo');

-- 3. Agregar las columnas faltantes
ALTER TABLE tiempos_procedimientos 
ADD COLUMN tiempo_minimo DECIMAL(10,2) DEFAULT 0.00 COMMENT 'Tiempo mínimo en horas';

ALTER TABLE tiempos_procedimientos 
ADD COLUMN tiempo_promedio DECIMAL(10,2) DEFAULT 0.00 COMMENT 'Tiempo promedio en horas';

ALTER TABLE tiempos_procedimientos 
ADD COLUMN tiempo_maximo DECIMAL(10,2) DEFAULT 0.00 COMMENT 'Tiempo máximo en horas';

-- 4. Verificar que se agregaron correctamente
DESCRIBE tiempos_procedimientos;

-- 5. Verificar las columnas de tiempo específicamente
SELECT 
    COLUMN_NAME,
    DATA_TYPE,
    IS_NULLABLE,
    COLUMN_DEFAULT,
    COLUMN_COMMENT
FROM INFORMATION_SCHEMA.COLUMNS 
WHERE TABLE_SCHEMA = 'cargas_trabajo' 
  AND TABLE_NAME = 'tiempos_procedimientos'
  AND COLUMN_NAME IN ('tiempo_minimo', 'tiempo_promedio', 'tiempo_maximo', 'tiempo_estandar')
ORDER BY ORDINAL_POSITION; 